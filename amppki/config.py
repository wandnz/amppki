""" Global config options that are used by both the web and cli scripts """

CA_DIR = "/etc/amplet2/pki"
CERT_DIR = "%s/certs" % CA_DIR
CSR_DIR = "%s/csr" % CA_DIR
RABBITMQCTL = "/usr/sbin/rabbitmqctl"
